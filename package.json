{
  "name": "passkeys-example",
  "version": "0.0.1",
  "description": "Passkeys examples",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "prebuild": "rm -rf build",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "predeploy": "npm run build && cp src/assets/* build/",
    "deploy": "gh-pages -d build"
  },
  "author": "Neal Fennimore",
  "license": "MIT",
  "dependencies": {
    "base64url": "^3.0.1"
  }
}
